# FUNCTION WITH PARAMETERS
# Meaning:
# Parameters allow us to pass values into a function,
# so the function can work with different data.

# Defining a function with parameters
def greet(name):
    print("Hello", name)

# Calling the function with different values
greet("Alice")
greet("Bob")
greet("Python")
